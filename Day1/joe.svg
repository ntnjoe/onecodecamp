<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 280 150">
	<defs>
    <style>
        @font-face {
            font-family: 'Source Code Pro';
            src: url('./SourceCodePro-Regular.ttf') format('truetype');
        }
    </style>
</defs>

	<text id="textElement" x="0" dominant-baseline="middle" y="50%" fill="black" font-family="Source Code Pro" font-size="60" style="user-select: none;"></text>
	<line id="lineElement" x1="3" y1="29.5" x2="3" y2="106" stroke="black" stroke-width="5" >
		<animate id="line-blink" attributeName="visibility" values="visible;hidden" dur="500ms" repeatCount="indefinite" />
	</line>
	<script>
		<![CDATA[
			const textElement = document.getElementById("textElement");
			const lineElement = document.getElementById("lineElement");
			const lineAnimation = document.getElementById("line-blink");
			const textContent = "joe.dev";
			let index = 0;
			setTimeout(typeWriter, 2000)
			function typeWriter() {
				var textWidth = textElement.getComputedTextLength();
				textElement.textContent += textContent[index];
				lineElement.setAttribute("x1", textWidth + 39);
				lineElement.setAttribute("x2", textWidth + 39);
				lineAnimation.beginElement()
				index++;
				if (index < textContent.length) {
					const array = [100, 300, 200];
					const speed = array[Math.floor(Math.random() * array.length)];
					console.log(speed);
					setTimeout(typeWriter, speed);
				}
			}
			
		]]>
	</script>
</svg>